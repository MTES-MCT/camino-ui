version: "3"

services:

  front:
    build: .
    environment:
      VIRTUAL_HOST: camino.pw
      VIRTUAL_PORT: ${NODE_PORT}
      LETSENCRYPT_HOST: api.camino.pw
      LETSENCRYPT_EMAIL: francoisromain@gmail.com
      NODE_PORT: ${NODE_PORT}
    expose:
      - ${NODE_PORT}

networks:
  default:
    external:
      name: nginx-proxy