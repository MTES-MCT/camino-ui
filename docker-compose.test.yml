version: "3"

services:

  app:
    build: .
    environment:
      VIRTUAL_HOST: camino.site
      VIRTUAL_PORT: ${NODE_PORT}
      LETSENCRYPT_HOST: camino.site
      LETSENCRYPT_EMAIL: francoisromain@gmail.com
    expose:
      - ${NODE_PORT}

networks:
  default:
    external:
      name: nginx-proxy