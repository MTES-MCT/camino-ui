version: '3'

services:
  app:
    image: node:alpine
    volumes:
      - ./:/app
    working_dir: /app
    environment:
      DEV_SERVER_DISABLE_HOST_CHECK: 1
    env_file: .env
    ports:
      - ${NODE_PORT}:${NODE_PORT}
    command: npm run dev
